<Formik
        initialValues={{
          name: '',
          number: '',
          email: ''
        }}
        onSubmit={(values) => {
          var data = {
            name: values.name,
            number:values.number,
            email:values.email
          }
          userData.push(data);
          // fetchData();
          console.log(userData);
        }}
      >
        {({
          values,
          errors,
          touched,
          handleAddFormChange,
          handleBlur,
          handleAddFormSubmit,
          isSubmitting,
          /* and other goodies */
        }) => (